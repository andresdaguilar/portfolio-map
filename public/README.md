# Files kept out of the repository

**`Andres_Aguilar_Resume.pdf`** — the résumé the site offers for download. It
is deliberately not committed: this repository is public and the PDF carries a
phone number.

The site links to `/Andres_Aguilar_Resume.pdf`, so the download works as soon
as the file is present. Either upload it to the host alongside the build, or
drop a version without the phone number into `public/` and commit that one
instead.

Nothing else breaks without it — only that one button.
