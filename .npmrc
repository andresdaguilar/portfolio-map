# @react-three/fiber v9 declares react-native as an optional peer dependency.
# npm tries to satisfy it by pulling the whole Expo toolchain into the tree.
# We only ever render to a DOM canvas, so peer resolution is relaxed here.
legacy-peer-deps=true
